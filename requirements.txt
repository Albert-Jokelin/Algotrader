# Core web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# HTTP client for tests and async
httpx>=0.25.0
aiohttp>=3.8.0

# Data validation and serialization
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Timezone handling
pytz>=2023.3

# Data analysis (backtesting)
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.0.0

# Broker API (existing upstoxlite package)
requests>=2.31.0
websocket-client>=1.6.0
websockets>=11.0
tenacity>=8.2.0

# Environment config
python-dotenv>=1.0.0

# ─── Testing ────────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
freezegun>=1.2.0          # Freeze time in tests
responses>=0.23.0          # Mock HTTP responses
aioresponses>=0.7.4        # Mock aiohttp responses
